# Backend
DEFAULT_COUNTRY="US" # 2 letter country code (e.g. US, GB, DE)
BACKEND_URL="https://example.com:9000"

# Postgres
POSTGRES_USER="postgres"
# "localhost" for dev or "postgres" for prod
POSTGRES_HOST="postgres"
POSTGRES_DB="store"

# Redis
# "redis://localhost:6379" for dev or "redis://redis:6379" for prod
REDIS_URL="redis://redis:6379"

# Cors
ADMIN_CORS="https://example.com"
STORE_CORS="http://frontend:8000,https://example.com"

# Meilisearch
# "http://localhost:7700" for dev or "http://meilisearch:7700" for prod
MEILI_HOST="http://meilisearch:7700"
MEILI_MASTER_KEY=""

# Auth cookies
JWT_SECRET=""
COOKIE_SECRET=""

# Email environment variables
EMAIL_SENDER_ADDRESS="noreply@example.com" # Email address to send notification emails from
EMAIL_SENDER_PASSWORD="" # Password for the sender email address
EMAIL_HOST="mail.example.com" # SMTP server host

# Frontend
NEXT_PUBLIC_COMPANY_NAME="" # Company name to display in the header, footer, and other places
NEXT_PUBLIC_COMPANY_DESCRIPTION="" # Company description to display in the header, footer, and other places

NEXT_PUBLIC_BACKEND_URL="https://example.com:9000"

# Stripe (Check out https://stripe.com/docs/keys for more information)
NEXT_PUBLIC_STRIPE_PUBLISH_KEY="pk_" # Stripe publishable key
STRIPE_API_KEY="sk_" # Stripe secret key
STRIPE_WEBHOOK_SECRET="whsec_" # Stripe webhook secret

# Guide for generating secrets

# You can ignore this if you used the ./prepare.sh script

# JWT_SECRET - openssl rand -base64 32
# COOKIE_SECRET - openssl rand -base64 64
# MEILI_MASTER_KEY - openssl rand -base64 32
